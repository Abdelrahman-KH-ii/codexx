SECRET_KEY=change-me-in-production
DEBUG=True
ALLOWED_HOSTS=localhost,127.0.0.1

# PostgreSQL (default)
DB_ENGINE=postgresql
DB_NAME=nexus_edu
DB_USER=postgres
DB_PASSWORD=postgres
DB_HOST=localhost
DB_PORT=5432

# Set DB_ENGINE=sqlite for local dev without PostgreSQL
# DB_ENGINE=sqlite
